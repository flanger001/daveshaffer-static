---
layout: post
title: Posts
permalink: /posts/
---
{% if collections.posts %}
  <h2 class="mb-2 text-xl sm:text-lg">Here are my latest posts:</h2>
  <ul class="list-outside list-disc px-4">
    {% assign sorted_posts = collections.posts | sort: "date" | reverse %}
    {% for post in sorted_posts %}
      <li class="mb-3 pl-1 leading-tight">
        {{ post.date | date: "%b %d, %Y" }}: <a href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
    {% endfor %}
  </ul>
{% endif %}
